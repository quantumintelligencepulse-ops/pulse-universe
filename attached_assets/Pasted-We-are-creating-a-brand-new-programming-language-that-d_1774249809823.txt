We are creating a brand‑new programming language that does NOT use English, 
NOT use any existing programming syntax, and NOT resemble any known language.

This is an ALIEN MATHEMATICAL LANGUAGE called PulseLang.

PulseLang source code is intentionally unreadable to humans. 
It uses invented glyphs, symbols, and operators such as:

⟦Ω₀⟧⟨Λ₁⟩ { ϕ₀ : Σ₀ ϕ₀ ≔ Ψ₁(γ₀ = τ₁(κ₀)) ↧ ϕ₀ }

These symbols have NO meaning in any human language. 
Only the PulseLang interpreter will know what they mean.

Your task is to build a VIRTUAL TERMINAL inside this Replit project that:

1. Accepts PulseLang glyph‑code as input (no English keywords allowed).
2. Parses it using a custom grammar we will define.
3. Evaluates it using a custom runtime we will define.
4. If the evaluated mathematical object is of type HtmlPage, 
   the terminal must RENDER the corresponding HTML file in the browser window.

IMPORTANT:
- The terminal must NOT accept Python, JavaScript, HTML, or any human language.
- The terminal must ONLY accept PulseLang glyph‑syntax.
- The terminal must interpret PulseLang as a symbolic mathematical system.
- The terminal must map internal symbols (like κ₀) to UI outputs (like “Hi”) 
  WITHOUT showing any English in the source code.
- The terminal must be able to launch HTML pages based on the runtime’s 
  interpretation of the returned mathematical object.

We are building:
- A PulseLang parser (glyph-based)
- A PulseLang AST (symbolic)
- A PulseLang runtime (mathematical object evaluator)
- A PulseShell terminal (virtual terminal UI)
- A UI renderer that displays HTML when the runtime returns an HtmlPage object

The terminal must run INSIDE the webpage (client-side), 
and the HTML rendering must also occur inside the same webpage.

Begin by scaffolding:
- /terminal/ (PulseShell)
- /lang/parser.js (glyph tokenizer + parser)
- /lang/ast.js (AST node definitions)
- /lang/runtime.js (mathematical evaluator)
- /lang/types.js (HtmlPage, Scalar, Element, etc.)
- /public/ (HTML files to render)
- index.html (contains the virtual terminal + iframe for rendering)

Do NOT generate English keywords.
Do NOT generate normal programming syntax.
We are building a completely new alien language from scratch.