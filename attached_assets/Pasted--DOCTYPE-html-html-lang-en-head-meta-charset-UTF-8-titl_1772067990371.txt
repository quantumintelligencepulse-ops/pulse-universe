<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{ARTICLE_TITLE} · Banking With Billy · 156‑Industry Newsroom</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="{ARTICLE_DEK_PLAIN}" />
  <meta property="og:title" content="{ARTICLE_TITLE}" />
  <meta property="og:description" content="{ARTICLE_DEK_PLAIN}" />
  <meta property="og:type" content="article" />
  <meta property="og:site_name" content="Banking With Billy" />
  <meta property="article:section" content="{INDUSTRY_NAME}" />
  <meta property="article:tag" content="{ARTICLE_TAGS_CSV}" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="{ARTICLE_TITLE}" />
  <meta name="twitter:description" content="{ARTICLE_DEK_PLAIN}" />

  <style>
    :root {
      --page-bg: #f4f4f7;
      --page-fg: #020617;
      --muted: #6b7280;
      --border-subtle: #e5e7eb;
      --accent: #0f766e;
      --accent-soft: #ccfbf1;
      --accent-strong: #0d9488;
      --headline: #020617;
      --card-bg: #ffffff;
      --card-soft: #f9fafb;
      --dark-bg: #020617;
      --dark-panel: #020617;
      --dark-panel-soft: #020617;
      --dark-border: #1f2937;
      --dark-muted: #9ca3af;
      --danger: #b91c1c;
      --ok: #16a34a;
      --warn: #d97706;
      --shadow-soft: 0 22px 60px rgba(15, 23, 42, 0.18);
      --radius-xl: 26px;
      --radius-lg: 18px;
      --radius-md: 12px;
      --radius-pill: 999px;
      --font-sans: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text",
        "Segoe UI", sans-serif;
    }

    * {
      box-sizing: border-box;
    }

    html,
    body {
      margin: 0;
      padding: 0;
      font-family: var(--font-sans);
      background:
        radial-gradient(circle at top, #e0f2fe 0, #f4f4f7 40%, #e5e7eb 100%);
      color: var(--page-fg);
      -webkit-font-smoothing: antialiased;
    }

    a {
      color: var(--accent-strong);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    .page-wrap {
      max-width: 1180px;
      margin: 0 auto;
      padding: 24px 16px 40px;
    }

    /* Masthead */

    .masthead {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 18px;
    }

    .brand-lockup {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .brand-title {
      font-size: 13px;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--muted);
    }

    .brand-title span {
      color: var(--accent-strong);
    }

    .brand-sub {
      font-size: 12px;
      color: var(--muted);
    }

    .masthead-meta {
      text-align: right;
      font-size: 11px;
      color: var(--muted);
    }

    .masthead-meta div + div {
      margin-top: 2px;
    }

    /* Pulse strip */

    .pulse-strip {
      margin-bottom: 22px;
      padding: 8px 12px;
      border-radius: var(--radius-pill);
      background: rgba(15, 23, 42, 0.03);
      border: 1px solid var(--border-subtle);
      display: flex;
      align-items: center;
      gap: 10px;
      overflow: hidden;
      white-space: nowrap;
    }

    .pulse-dot {
      width: 9px;
      height: 9px;
      border-radius: 999px;
      background: var(--accent-strong);
      box-shadow: 0 0 0 0 rgba(13, 148, 136, 0.6);
      animation: pulse 1.8s infinite;
    }

    .pulse-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      color: var(--muted);
    }

    .pulse-text {
      font-size: 12px;
      color: #111827;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .pulse-right {
      margin-left: auto;
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      color: var(--muted);
    }

    @keyframes pulse {
      0% {
        box-shadow: 0 0 0 0 rgba(13, 148, 136, 0.6);
      }
      70% {
        box-shadow: 0 0 0 10px rgba(13, 148, 136, 0);
      }
      100% {
        box-shadow: 0 0 0 0 rgba(13, 148, 136, 0);
      }
    }

    /* HYBRID HERO: light editorial + dark intelligence bar */

    .hero-shell {
      border-radius: var(--radius-xl);
      overflow: hidden;
      box-shadow: var(--shadow-soft);
      border: 1px solid rgba(148, 163, 184, 0.25);
      margin-bottom: 26px;
      background: linear-gradient(
        180deg,
        #ffffff 0,
        #f9fafb 52%,
        #020617 52%,
        #020617 100%
      );
    }

    .hero-top {
      padding: 24px 24px 18px;
      display: grid;
      grid-template-columns: minmax(0, 1.7fr) minmax(0, 1.1fr);
      gap: 22px;
    }

    .hero-main {
      position: relative;
      z-index: 1;
    }

    .hero-kicker {
      font-size: 11px;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--accent-strong);
      margin-bottom: 8px;
    }

    .hero-title {
      font-size: 30px;
      line-height: 1.15;
      letter-spacing: -0.02em;
      color: var(--headline);
      margin: 0 0 8px;
    }

    .hero-dek {
      font-size: 15px;
      line-height: 1.6;
      color: #4b5563;
      margin: 0 0 14px;
      max-width: 640px;
    }

    .hero-meta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px 16px;
      align-items: center;
      margin-bottom: 14px;
      font-size: 12px;
      color: var(--muted);
    }

    .hero-meta-row span {
      display: inline-flex;
      align-items: center;
      gap: 4px;
    }

    .hero-meta-row .dot {
      width: 3px;
      height: 3px;
      border-radius: 999px;
      background: var(--muted);
    }

    .hero-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-bottom: 4px;
    }

    .hero-tag {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      padding: 4px 9px;
      border-radius: var(--radius-pill);
      border: 1px solid var(--border-subtle);
      background: var(--card-soft);
      color: #374151;
    }

    .hero-tag.primary {
      border-color: var(--accent-strong);
      background: var(--accent-soft);
      color: var(--accent-strong);
    }

    .hero-byline {
      font-size: 11px;
      color: var(--muted);
      margin-top: 4px;
    }

    .hero-media {
      position: relative;
      border-radius: 22px;
      overflow: hidden;
      background: radial-gradient(circle at top, #e0f2fe 0, #eff6ff 40%, #f9fafb 100%);
      border: 1px solid rgba(148, 163, 184, 0.35);
      display: flex;
      flex-direction: column;
      gap: 10px;
      padding: 10px;
    }

    .hero-image-frame {
      position: relative;
      border-radius: 18px;
      overflow: hidden;
      background: #020617;
      box-shadow: 0 18px 40px rgba(15, 23, 42, 0.35);
    }

    .hero-image-frame img {
      width: 100%;
      height: 100%;
      display: block;
      object-fit: cover;
    }

    .hero-image-caption {
      font-size: 11px;
      color: var(--muted);
      padding: 4px 4px 2px;
    }

    /* Dark intelligence bar under hero */

    .hero-intel-bar {
      background: radial-gradient(circle at top, #0b1120 0, #020617 55%, #020617 100%);
      border-top: 1px solid rgba(148, 163, 184, 0.35);
      padding: 10px 18px 12px;
      display: grid;
      grid-template-columns: minmax(0, 1.6fr) minmax(0, 1.4fr);
      gap: 12px;
      color: #e5e7eb;
    }

    .intel-left-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      color: #9ca3af;
      margin-bottom: 4px;
    }

    .intel-left-main {
      font-size: 13px;
      color: #e5e7eb;
    }

    .intel-left-main span {
      color: var(--accent-soft);
    }

    .intel-right-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
      font-size: 11px;
    }

    .intel-pill {
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.6);
      padding: 6px 8px;
      background: rgba(15, 23, 42, 0.85);
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .intel-pill-label {
      text-transform: uppercase;
      letter-spacing: 0.16em;
      color: #9ca3af;
      font-size: 9px;
    }

    .intel-pill-value {
      font-size: 11px;
      color: #e5e7eb;
    }

    /* Sticky intelligence header (hybrid) */

    .sticky-intel {
      position: sticky;
      top: 0;
      z-index: 20;
      backdrop-filter: blur(16px);
      background: linear-gradient(
        180deg,
        rgba(248, 250, 252, 0.92),
        rgba(248, 250, 252, 0.86),
        rgba(248, 250, 252, 0.0)
      );
      border-bottom: 1px solid rgba(209, 213, 219, 0.7);
      padding: 8px 4px 6px;
      margin-bottom: 10px;
    }

    .sticky-intel-inner {
      max-width: 1180px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 11px;
      color: var(--muted);
    }

    .sticky-intel-label {
      text-transform: uppercase;
      letter-spacing: 0.18em;
      font-size: 10px;
    }

    .sticky-intel-dot {
      width: 6px;
      height: 6px;
      border-radius: 999px;
      background: var(--accent-strong);
    }

    .sticky-intel-main {
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      flex: 1;
    }

    .sticky-intel-meta {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.16em;
    }

    /* Main layout */

    .layout {
      display: grid;
      grid-template-columns: minmax(0, 2.1fr) minmax(0, 1.1fr);
      gap: 22px;
      align-items: flex-start;
    }

    .article-main {
      display: flex;
      flex-direction: column;
      gap: 18px;
    }

    .article-card {
      background: var(--card-bg);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-subtle);
      box-shadow: var(--shadow-soft);
      padding: 18px 20px 20px;
    }

    .article-card-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 10px;
    }

    .article-card-icon {
      width: 26px;
      height: 26px;
      border-radius: 999px;
      background: var(--card-soft);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      border: 1px solid rgba(148, 163, 184, 0.6);
    }

    .article-card-title {
      font-size: 13px;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: #111827;
    }

    .article-card-sub {
      font-size: 12px;
      color: var(--muted);
      margin-bottom: 10px;
    }

    /* Article body (editorial) */

    .article-body {
      font-size: 15px;
      line-height: 1.7;
      color: #111827;
      column-gap: 32px;
    }

    @media (min-width: 960px) {
      .article-body {
        column-count: 2;
      }
    }

    .article-body p {
      margin: 0 0 12px;
      break-inside: avoid;
    }

    .article-body h2 {
      font-size: 18px;
      margin: 18px 0 8px;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: #111827;
      break-inside: avoid;
    }

    .article-body h3 {
      font-size: 15px;
      margin: 14px 0 6px;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: #111827;
      break-inside: avoid;
    }

    .article-body ul,
    .article-body ol {
      margin: 0 0 12px 18px;
      padding: 0;
      break-inside: avoid;
    }

    .article-body li {
      margin-bottom: 6px;
    }

    .article-body blockquote {
      margin: 12px 0;
      padding: 10px 14px;
      border-left: 3px solid var(--accent-strong);
      background: #ecfeff;
      font-size: 14px;
      color: #0f172a;
      break-inside: avoid;
    }

    .article-body code {
      font-family: ui-monospace, Menlo, Monaco, Consolas, "Courier New", monospace;
      font-size: 13px;
      background: #e5e7eb;
      padding: 1px 4px;
      border-radius: 4px;
    }

    /* Dark data / timeline / sources panel */

    .dark-card {
      background: radial-gradient(circle at top, #020617 0, #020617 100%);
      border-radius: var(--radius-lg);
      border: 1px solid var(--dark-border);
      box-shadow: 0 22px 60px rgba(0, 0, 0, 0.7);
      padding: 18px 20px 20px;
      color: #e5e7eb;
    }

    .dark-card-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 10px;
    }

    .dark-card-icon {
      width: 26px;
      height: 26px;
      border-radius: 999px;
      background: #020617;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      color: #e5e7eb;
    }

    .dark-card-title {
      font-size: 13px;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: #f9fafb;
    }

    .dark-card-sub {
      font-size: 12px;
      color: var(--dark-muted);
      margin-bottom: 10px;
    }

    .data-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.1fr);
      gap: 14px;
      margin-top: 8px;
    }

    .data-box {
      border-radius: var(--radius-md);
      background: #020617;
      border: 1px solid rgba(148, 163, 184, 0.7);
      padding: 10px 12px;
      font-size: 13px;
      color: #e5e7eb;
    }

    .data-box h4 {
      margin: 0 0 6px;
      font-size: 11px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: #9ca3af;
    }

    .data-box pre {
      margin: 0;
      font-family: ui-monospace, Menlo, Monaco, Consolas, "Courier New", monospace;
      font-size: 12px;
      white-space: pre-wrap;
      word-wrap: break-word;
      max-height: 180px;
      overflow-y: auto;
    }

    .source-list {
      margin: 6px 0 0;
      padding-left: 16px;
      font-size: 13px;
    }

    .source-list li {
      margin-bottom: 4px;
      word-wrap: break-word;
    }

    /* Verdict / signal */

    .signal-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
      margin-top: 8px;
    }

    .signal-item {
      display: flex;
      flex-direction: column;
      gap: 4px;
      font-size: 12px;
    }

    .signal-label {
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--muted);
    }

    .signal-bar {
      height: 8px;
      border-radius: 999px;
      background: #e5e7eb;
      overflow: hidden;
      position: relative;
    }

    .signal-bar span {
      position: absolute;
      inset: 0;
      border-radius: inherit;
    }

    .signal-high span {
      width: 100%;
      background: linear-gradient(90deg, #22c55e, #4ade80);
    }

    .signal-medium span {
      width: 60%;
      background: linear-gradient(90deg, #facc15, #f97316);
    }

    .signal-low span {
      width: 30%;
      background: linear-gradient(90deg, #9ca3af, #6b7280);
    }

    .signal-note {
      font-size: 12px;
      color: var(--muted);
      margin-top: 8px;
    }

    /* Right rail */

    .rail {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .rail-card {
      background: var(--card-bg);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-subtle);
      box-shadow: var(--shadow-soft);
      padding: 14px 14px 16px;
      font-size: 13px;
    }

    .rail-title {
      font-size: 11px;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: #111827;
      margin-bottom: 6px;
    }

    .rail-sub {
      font-size: 12px;
      color: var(--muted);
      margin-bottom: 8px;
    }

    .rail-list {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    .rail-list li + li {
      margin-top: 8px;
    }

    .rail-link {
      display: block;
      font-size: 13px;
      color: #111827;
    }

    .rail-link span {
      display: block;
      font-size: 11px;
      color: var(--muted);
      margin-top: 2px;
    }

    .rail-pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 4px 8px;
      border-radius: var(--radius-pill);
      background: var(--card-soft);
      border: 1px solid var(--border-subtle);
      font-size: 11px;
      color: var(--muted);
      margin-top: 6px;
    }

    .rail-pill strong {
      color: #111827;
    }

    .video-embed {
      position: relative;
      width: 100%;
      border-radius: 14px;
      overflow: hidden;
      background: #020617;
      aspect-ratio: 16 / 9;
      box-shadow: 0 14px 30px rgba(15, 23, 42, 0.35);
    }

    .video-embed iframe,
    .video-embed video {
      width: 100%;
      height: 100%;
      border: 0;
      object-fit: cover;
    }

    /* Machine metadata */

    .machine-box {
      font-family: ui-monospace, Menlo, Monaco, Consolas, "Courier New", monospace;
      font-size: 11px;
      white-space: pre-wrap;
      word-wrap: break-word;
      background: var(--card-soft);
      border-radius: 10px;
      padding: 8px 10px;
      border: 1px solid var(--border-subtle);
      max-height: 180px;
      overflow-y: auto;
    }

    /* Footer */

    .page-footer {
      margin-top: 28px;
      padding-top: 16px;
      border-top: 1px solid var(--border-subtle);
      font-size: 11px;
      color: var(--muted);
      text-align: center;
      line-height: 1.6;
    }

    .page-footer span {
      color: var(--accent-strong);
    }

    @media (max-width: 960px) {
      .hero-top {
        grid-template-columns: minmax(0, 1fr);
      }
      .hero-intel-bar {
        grid-template-columns: minmax(0, 1fr);
      }
      .intel-right-grid {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
      .layout {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    @media (max-width: 720px) {
      .intel-right-grid {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    @media (max-width: 640px) {
      .hero-title {
        font-size: 24px;
      }
      .hero-dek {
        font-size: 14px;
      }
      .masthead {
        flex-direction: column;
        align-items: flex-start;
      }
      .masthead-meta {
        text-align: left;
      }
      .pulse-strip {
        flex-wrap: wrap;
        white-space: normal;
      }
      .pulse-right {
        margin-left: 0;
      }
      .article-body {
        column-count: 1;
      }
    }
  </style>
</head>
<body>
  <!-- Sticky hybrid intelligence header -->
  <div class="sticky-intel">
    <div class="sticky-intel-inner">
      <div class="sticky-intel-dot"></div>
      <div class="sticky-intel-label">Live Industry Signal</div>
      <div class="sticky-intel-main">{STICKY_SIGNAL_TEXT}</div>
      <div class="sticky-intel-meta">{SECTOR_NAME} · {INDUSTRY_NAME}</div>
    </div>
  </div>

  <div class="page-wrap">
    <!-- Masthead -->
    <header class="masthead">
      <div class="brand-lockup">
        <div class="brand-title">
          <span>Banking With Billy</span> · 156‑Industry Newsroom
        </div>
        <div class="brand-sub">
          Real‑time reporting across all 156 industries · Verified, structured, machine‑readable.
        </div>
      </div>
      <div class="masthead-meta">
        <div>{NEWSROOM_EDITION}</div>
        <div>{NEWSROOM_DATELINE}</div>
      </div>
    </header>

    <!-- Pulse strip -->
    <div class="pulse-strip">
      <div class="pulse-dot"></div>
      <div class="pulse-label">Live Signal</div>
      <div class="pulse-text">{PULSE_TEXT}</div>
      <div class="pulse-right">{SECTOR_NAME} · {INDUSTRY_NAME}</div>
    </div>

    <!-- HYBRID HERO -->
    <article class="hero-shell">
      <div class="hero-top">
        <div class="hero-main">
          <div class="hero-kicker">{INDUSTRY_KICKER}</div>
          <h1 class="hero-title">{ARTICLE_TITLE}</h1>
          <p class="hero-dek">{ARTICLE_DEK}</p>

          <div class="hero-meta-row">
            <span>By <strong>{ARTICLE_AUTHOR}</strong></span>
            <span class="dot"></span>
            <span>Published {ARTICLE_PUBLISHED_AT}</span>
            <span class="dot"></span>
            <span>Updated {ARTICLE_UPDATED_AT}</span>
            <span class="dot"></span>
            <span>{ARTICLE_READING_TIME} min read</span>
          </div>

          <div class="hero-tags">
            <span class="hero-tag primary">{SECTOR_NAME}</span>
            <span class="hero-tag">{INDUSTRY_GROUP_NAME}</span>
            <span class="hero-tag">{INDUSTRY_NAME}</span>
            {ARTICLE_TAG_BADGES}
          </div>

          <div class="hero-byline">
            Part of the Banking With Billy 156‑Industry Intelligence Network. This article is continuously updated as new data arrives.
          </div>
        </div>

        <aside class="hero-media">
          <div class="hero-image-frame">
            {HERO_MEDIA}
          </div>
          <div class="hero-image-caption">{HERO_MEDIA_CAPTION}</div>
        </aside>
      </div>

      <div class="hero-intel-bar">
        <div>
          <div class="intel-left-label">Story Position</div>
          <div class="intel-left-main">
            <span>{INTEL_POSITION_LABEL}</span> · {INTEL_POSITION_CONTEXT}
          </div>
        </div>
        <div class="intel-right-grid">
          <div class="intel-pill">
            <div class="intel-pill-label">Market</div>
            <div class="intel-pill-value">{MARKET_SHORT_LABEL}: {MARKET_SHORT_VALUE}</div>
          </div>
          <div class="intel-pill">
            <div class="intel-pill-label">Operational</div>
            <div class="intel-pill-value">{OP_SHORT_LABEL}: {OP_SHORT_VALUE}</div>
          </div>
          <div class="intel-pill">
            <div class="intel-pill-label">Regulatory</div>
            <div class="intel-pill-value">{REG_SHORT_LABEL}: {REG_SHORT_VALUE}</div>
          </div>
        </div>
      </div>
    </article>

    <!-- Main layout -->
    <div class="layout">
      <!-- Left: article + data -->
      <main class="article-main">
        <!-- Article body -->
        <section class="article-card">
          <div class="article-card-header">
            <div class="article-card-icon">📰</div>
            <div class="article-card-title">Story</div>
          </div>
          <div class="article-card-sub">
            Narrative coverage of this development, written for humans and structured for machines.
          </div>
          <div class="article-body">
            {ARTICLE_BODY_HTML}
          </div>
        </section>

        <!-- Dark data / timeline / sources -->
        <section class="dark-card">
          <div class="dark-card-header">
            <div class="dark-card-icon">📊</div>
            <div class="dark-card-title">Data & Timeline</div>
          </div>
          <div class="dark-card-sub">
            Structured context for this story — numbers, chronology, and the sources that anchor it.
          </div>

          <div class="data-grid">
            <div class="data-box">
              <h4>Key Metrics</h4>
              <pre>{KEY_METRICS_TEXT}</pre>
            </div>
            <div class="data-box">
              <h4>Timeline</h4>
              <pre>{TIMELINE_TEXT}</pre>
            </div>
          </div>

          <div class="data-box" style="margin-top: 12px;">
            <h4>Primary Sources</h4>
            <ul class="source-list">
              {SOURCE_LINKS_HTML}
            </ul>
          </div>
        </section>

        <!-- Verdict / signal -->
        <section class="article-card">
          <div class="article-card-header">
            <div class="article-card-icon">🧭</div>
            <div class="article-card-title">Banking With Billy Intelligence Verdict</div>
          </div>
          <div class="article-card-sub">
            A three‑axis signal — not a rating, but a live read on how this story moves the industry.
          </div>

          <div class="signal-grid">
            <div class="signal-item">
              <div class="signal-label">Operational Impact</div>
              <div class="signal-bar signal-{SIGNAL_OP_LEVEL}">
                <span></span>
              </div>
              <div>{SIGNAL_OP_LABEL}</div>
            </div>
            <div class="signal-item">
              <div class="signal-label">Market Impact</div>
              <div class="signal-bar signal-{SIGNAL_MKT_LEVEL}">
                <span></span>
              </div>
              <div>{SIGNAL_MKT_LABEL}</div>
            </div>
            <div class="signal-item">
              <div class="signal-label">Regulatory / Policy</div>
              <div class="signal-bar signal-{SIGNAL_REG_LEVEL}">
                <span></span>
              </div>
              <div>{SIGNAL_REG_LABEL}</div>
            </div>
          </div>

          <div class="signal-note">
            {SIGNAL_SUMMARY_TEXT}
          </div>
        </section>
      </main>

      <!-- Right rail -->
      <aside class="rail">
        <!-- Video / live -->
        <section class="rail-card">
          <div class="rail-title">Live & Video</div>
          <div class="rail-sub">
            Latest video briefings, explainers, and live coverage tied to this story.
          </div>
          <div class="video-embed">
            {VIDEO_EMBED_HTML}
          </div>
          <div class="rail-pill">
            <strong>{VIDEO_PRIMARY_LABEL}</strong>
            <span>{VIDEO_SECONDARY_LABEL}</span>
          </div>
        </section>

        <!-- Related stories -->
        <section class="rail-card">
          <div class="rail-title">Related Coverage</div>
          <div class="rail-sub">
            Additional stories from the same sector, group, or cross‑industry impact.
          </div>
          <ul class="rail-list">
            {RELATED_STORIES_HTML}
          </ul>
        </section>

        <!-- Industry snapshot -->
        <section class="rail-card">
          <div class="rail-title">Industry Snapshot</div>
          <div class="rail-sub">
            Where this story sits inside the 156‑industry map.
          </div>
          <div>
            <strong>Sector:</strong> {SECTOR_NAME}<br />
            <strong>Industry Group:</strong> {INDUSTRY_GROUP_NAME}<br />
            <strong>Industry:</strong> {INDUSTRY_NAME}
          </div>
          <div class="rail-pill">
            <strong>{INDUSTRY_STATUS_LABEL}</strong>
            <span>{INDUSTRY_STATUS_CONTEXT}</span>
          </div>
        </section>

        <!-- Machine metadata -->
        <section class="rail-card">
          <div class="rail-title">Machine Notes</div>
          <div class="rail-sub">
            Structured metadata for downstream systems, feeds, and archives.
          </div>
          <div class="machine-box">
{MACHINE_METADATA_JSON}
          </div>
        </section>
      </aside>
    </div>

    <!-- Footer -->
    <footer class="page-footer">
      © {ARTICLE_YEAR} <span>Banking With Billy</span>.  
      Part of the 156‑Industry Newsroom — continuously updated, machine‑readable, and independently operated.
    </footer>
  </div>
</body>
</html>