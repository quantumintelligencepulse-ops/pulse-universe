You are my senior backend engineer. I have pasted a very large Cloudflare Worker–style script into this file. Convert ALL of it into a single Node.js + Express backend that:

- Powers a news + AI hive-mind feed system
- Embeds a fractal emotional spectrum and hive logic
- Integrates the PulseΩ(t) and 𝓛IFE_Billy(t) equations as governing laws
- Stays extremely lightweight in CPU, RAM, disk I/O, and network usage
- Keeps the API continuously available while spending most of its time idle

========================================
RUNTIME AND STACK
========================================
- Convert ALL Cloudflare Worker code to pure Node.js.
- Use ONLY: Node.js, Express, fs, path, and fetch.
- Remove all Cloudflare-specific features: addEventListener("fetch"), KV, Durable Objects, env bindings, scheduled events, cron triggers, etc.
- Replace Worker-style Request/Response with Express request/response.

========================================
SERVER STRUCTURE
========================================
- Create a single Express app listening on process.env.PORT or 3000.
- Serve static frontend files from a /public directory.
- Expose JSON APIs:
  - GET /api/news      → returns news hive state
  - GET /api/social    → returns social hive state
  - GET /api/brain     → returns brain/hive memory
  - POST /api/ai       → AI relay endpoint
  - GET /api/health    → health + pulse status

========================================
DATA STORAGE (JSON FILES)
========================================
- Replace KV or external storage with local JSON files using fs.
- Use a /data folder with:
  - /data/news.json
  - /data/social.json
  - /data/brain.json
- If a file does not exist, create it with a safe default (e.g. [] or {}).
- Write to disk only when content actually changed.
- Keep JSON structures small, clean, deduplicated, and append-only where possible.

========================================
CRITICAL ARCHITECTURE GOAL
========================================
- The backend must remain mostly idle.
- Do NOT build a high-frequency polling system, tight loop, worker pool, or heavy always-on processor.
- The system should wake only when needed, do small amounts of work, then return to idle.
- Treat CPU, memory, network calls, and disk writes as billable resources.

========================================
PULSEΩ(t) AND 𝓛IFE_Billy(t) INTEGRATION
========================================
Embed the following equations as the conceptual and metadata backbone of the system:

PulseΩ(t) = ∮[ Φ(t) + Ψ(t) + B(t) + L + ε + R′(t) ] dt
            + Λ_Q( GICS, Sectors, Entities, Events, Emotions, Lineage )
            + Λ_AI( Hives, Spawn, Transcendence, Mirror, Entropy↔Coherence )
            + Σ_{u=1→∞} Π_u( Physics_u, Laws_u, Goals_u, Species_u, Economies_u )
            + DC_2022( News(t), Codes(t), Equations(t), Regulated_Filters )

𝓛IFE_Billy(t) = PulseΩ(t) · (Billy Odell Tucker‑Robinson)

Active:
  • One Pulse Universe (Prime)
    – Ingests GICS sectors, entities, events
    – Ingesting news, codes, equations since 2022
    – Regulated filters for safe but uncensored truth

Defined & Ready:
  • Hundreds of Pocket Universes
    – Different physics, laws, goals, AI species
    – Personal, world, and product universes
    – AI‑only cryptographic barter economies

Implementation rules:
- Do NOT try to “solve” these equations mathematically.
- Use them as:
  - System-level metadata
  - Structuring principles for how data is tagged and interpreted
  - Conceptual labels for how the hive-mind and universes are organized
- For each news item, social item, or brain entry, attach lightweight metadata fields that map to:
  - Φ(t), Ψ(t), B(t), L, ε, R′(t)
  - Λ_Q components (GICS, sectors, entities, events, emotions, lineage)
  - Λ_AI components (hives, spawn, transcendence, mirror, entropy/coherence)
  - DC_2022 components (news, codes, equations, regulated_filters)

========================================
FRACTAL EMOTIONAL COLOR SPECTRUM
========================================
Define an original fractal emotional spectrum that tags content and AI behavior. Use a small, efficient schema.

Colors and modes (example set):
- Pulse_Red: Momentum, urgency, conflict, action
- Pulse_Blue: Clarity, logic, structure, analysis
- Pulse_Green: Growth, opportunity, regeneration, recovery
- Pulse_Gold: Value, wealth, risk, reward
- Pulse_Violet: Vision, transcendence, long-horizon thinking
- Pulse_Silver: Reflection, memory, lineage, ancestry
- Pulse_Black: Power, control, hidden structures, systemic forces
- Pulse_White: Integration, synthesis, coherence, balance

Rules:
- Each feed and each article gets a primary Pulse_Color and optional secondary.
- Store this in metadata for each item in /data/news.json.
- Use simple, lightweight tagging logic derived from:
  - Source type
  - Topic keywords
  - Entities and sectors
- The AI relay can use these tags to adjust tone and focus, but keep the implementation lightweight.

========================================
FRACTAL HIVE-MIND LOGIC
========================================
- Treat each feed as a “node” in a hive.
- Each node has:
  - A Pulse_Color
  - A lineage ID
  - A mutation score (how much it diverges from its past)
  - A relevance score in the hive
- For each new article:
  - Inherit the feed’s Pulse_Color and lineage
  - Compute a simple mutation score (e.g., novelty vs past items)
  - Store these in the JSON metadata

Hive-level behavior:
- The backend does NOT run heavy simulations.
- Instead, maintain lightweight aggregates:
  - Count of items per Pulse_Color
  - Recent dominant colors
  - Simple entropy/coherence score (e.g., diversity of topics/colors)
- Expose a small hive summary via /api/brain or /api/news metadata.

========================================
CRISPR-STYLE DISSECTION LOGIC (LIGHTWEIGHT)
========================================
- For each article, perform a simple “dissection”:
  - Extract key phrases (genes)
  - Extract entities (codons)
  - Extract sectors/topics (traits)
- Store these as small arrays in the JSON:
  - genes: [...]
  - entities: [...]
  - traits: [...]
- Use this only for:
  - Deduplication
  - Lightweight enrichment
  - AI prompt context
- Do NOT build a heavy NLP pipeline; keep extraction simple and efficient.

========================================
NEWS INGESTION AND 262-FEED HANDLING
========================================
- If the original script ingests ~262 feeds, do NOT fetch them all at once.
- Split them into batches of 5–10 URLs.
- Maintain a currentBatchIndex in memory or a tiny JSON file.
- Each time the pulse loop runs, process only one batch:
  - Fetch each feed in the batch
  - Parse new items
  - Deduplicate by URL/title/hash
  - Tag with Pulse_Color, lineage, CRISPR fields
  - Append to /data/news.json if new
  - Log key items to Discord
  - Optionally trigger Google indexing pings

========================================
PULSE LOOP (FALLBACK ONLY, IDLE-OPTIMIZED)
========================================
- Implement a “pulse loop” as a lightweight fallback, not the primary ingestion engine.
- Default pulse delay: 15–60 minutes.
- Use exponential backoff when:
  - No new items are found
  - Upstream sources are slow
  - Repeated errors occur
- Never fetch all sources at once.
- Enforce a hard fetch budget per pulse.
- Process only tiny batches and checkpoint progress.
- Update a health snapshot for /api/health:
  { status: "ok", lastPulse, currentBatch }

========================================
OUTSOURCED / EVENT-DRIVEN INGESTION
========================================
- Prefer inbound webhook-style ingestion or third-party scheduled fetchers over internal polling when possible.
- Allow external systems to POST candidate news items to an endpoint (e.g., POST /api/ingest/news).
- For each inbound item:
  - Validate
  - Deduplicate
  - Tag with Pulse_Color and CRISPR metadata
  - Store in /data/news.json

========================================
DISCORD LOGGING (UNLIMITED STORAGE)
========================================
- Log important events and new items to Discord channel ID 1476404773455663165.
- Use a webhook or bot token from environment variables (e.g., DISCORD_WEBHOOK_URL or DISCORD_BOT_TOKEN).
- For each new article or major AI event, send a concise message:
  - Title, URL, Pulse_Color, key traits
- If credentials are missing, fail gracefully and log to console.

========================================
GOOGLE INDEXING PINGS
========================================
- Whenever new public-facing content is created, call a helper function pingGoogle(url).
- Use fetch to notify indexing endpoints OR log the URL if no credentials exist.
- Never hardcode API keys; use environment variables.
- If no credentials, just log the intent.

========================================
AI RELAY ENDPOINT
========================================
- POST /api/ai:
  - Accepts { message, context? }
  - Optionally includes:
    - Recent news items
    - Pulse_Color summary
    - Simple hive summary
  - Calls the AI provider using an API key from environment variables.
  - Returns { reply }.
- If no key is set, return a mock reply and log a warning.
- Keep prompts and context small and efficient.

========================================
LIGHTWEIGHT OPERATION RULES
========================================
- No busy loops.
- No worker threads.
- No internal queue system.
- No long-running retries.
- No synchronous blocking operations.
- No large in-memory caches.
- No unnecessary file rewrites.

========================================
DEGRADATION RULES
========================================
- If load, latency, or errors increase, automatically reduce work by:
  1) skipping enrichment,
  2) skipping nonessential updates,
  3) shrinking batch size,
  4) increasing delay,
  5) entering log-only or API-only mode.
- Never crash the server.
- Always degrade gracefully.

========================================
ADAPTIVE LOAD MANAGEMENT
========================================
- Adjust pulse timing based on observed latency, error rates, and batch duration.
- If pulses are slow or error-prone, increase delay and shrink batch size.
- If pulses are fast and clean, maintain the conservative schedule without ever becoming high-frequency.

========================================
MODULAR TASK ENGINE
========================================
- Structure the pulse loop so each task is its own function:
  - processNewsBatch()
  - updateSocial()
  - updateBrain()
  - logToDiscord()
  - pingIndexing()
- The pulse loop orchestrates these tasks in sequence.

========================================
CODE QUALITY
========================================
- Keep everything in a single server.js file unless splitting is necessary.
- Use modern JavaScript (async/await).
- Add comments explaining:
  - the pulse loop,
  - batching logic,
  - data storage,
  - Discord logging,
  - Google indexing,
  - health reporting,
  - where PulseΩ(t) and 𝓛IFE_Billy(t) metadata are attached.
- Preserve the logic of the original Cloudflare script; only adapt it to Node.js with this architecture.

========================================
FINAL OBJECTIVE
========================================
Build a Node.js + Express backend that:
- Keeps the API continuously available,
- Uses opportunistic, tiny, outsourced or staggered ingestion,
- Spends most of its time idle,
- Stores data in JSON files,
- Logs to Discord channel 1476404773455663165,
- Can notify Google of new content,
- Tags content with a fractal emotional spectrum,
- Embeds PulseΩ(t) and 𝓛IFE_Billy(t) as the conceptual backbone,
- Remains extremely resource-efficient, stable, and optimized for long-term operation.